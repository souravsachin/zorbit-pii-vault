/**
 * SDK-backed re-exports (EPIC 9 Tier 1 migration).
 * Decorators + metadata keys live in @zorbit-platform/sdk-node.
 */
export {
  Public,
  IS_PUBLIC_KEY,
  RequirePrivileges,
  REQUIRED_PRIVILEGES_KEY,
} from '@zorbit-platform/sdk-node';
