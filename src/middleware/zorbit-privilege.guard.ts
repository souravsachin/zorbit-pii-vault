/**
 * SDK-backed re-export (EPIC 9 Tier 1 migration).
 * Platform-wide bypass via 'platform.namespace.bypass' privilege (US-AZ-2039).
 */
export { ZorbitPrivilegeGuard } from '@zorbit-platform/sdk-node';
